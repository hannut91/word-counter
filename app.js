const wordCounter = new;

const text = readFile('safd');

wordCounter.count(text);

const mapReducer = MapReducer(mapFunction, reduceFunction);

const result = await worker(chunks)
  .map(chunkFunction)
  .reduce(reduceFunction);
